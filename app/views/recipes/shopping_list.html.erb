<h1>shopping list</h1>

  <% @items.each do |item| %>
  <br>
  <%= item.food.name%>, <%= item.quantity - item.food.quantity %>, $<%= (item.quantity - item.food.quantity)*item.food.price %>
  <% end %>